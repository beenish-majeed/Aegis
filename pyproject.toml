[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "aegis"
version = "0.1.0"
description = "Framework-agnostic RAG Faithfulness Auditor"
readme = "README.md"
requires-python = ">=3.11"
authors = [
    { name = "Beenish Majeed" }
]

dependencies = []

[tool.setuptools.packages.find]
where = ["."]